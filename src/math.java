package com.JAVA;
import java.util.Arrays;
//public class math {
//public static void main(String[] args) {
    //Maths
    
    // Finding the maximum value between 590999 and 1000
//    System.out.println(Math.max(590999, 1000)); // Output: 590999 (because 590999 is larger than 1000)
    
    // Finding the minimum value between 100 and 9999
//    System.out.println(Math.min(100, 9999)); // Output: 100 (because 100 is smaller than 9999)
    
    // Generating a random integer between 0 and 99
//    System.out.println((int)(Math.random() * 100)); // Output: a random integer between 0 and 99
    // Explanation: Math.random() generates a number between 0 and 1, then multiply it by 100 for range 0-99
    // Casting it to int truncates the decimal part (rounds down the value)
    
//}
//}
